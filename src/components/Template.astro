---
// 组件脚本（JavaScript）
/**
* 导入其他 Astro 组件
* 导入其他框架组件，如 React
* 导入数据，如 JSON 文件
* 从 API 或数据库中获取内容
* 创建你要在模板中引用的变量
*/
// 访问传入的组件参数，如 `<X title="Hello, World"/>`
// const {title} = Astro.props;
// 获取外部数据，甚至可以从私有 API 和数据库中获取
// const data = await fetch ('SOME_SECRET_API_URL/users').then (r => r.json ());
---
<!-- 组件模板（HTML + JS 表达式 + CSS Style）-->
